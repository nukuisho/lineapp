# requirements.md

## 1. 文書の目的

本書は、「援農ボランティア参加管理LINEアプリ」の目的、対象利用者、MVPの範囲、機能要件、画面要件、データ要件および受入条件を定義する。

Codexを含む開発者は、本書をプロジェクト要件の基準として参照する。

開発ルールは`AGENTS.md`を参照する。

LINE、LIFF、Firestoreなどの公式仕様は`official-references.md`を参照する。

---

## 2. プロジェクト概要

### 2.1 プロジェクト名

援農ボランティア参加管理LINEアプリ

仮称：

* 援農パスポート
* 援農スタンプ
* いなぎ援農パスポート

正式名称は未定とする。

### 2.2 背景

現在の援農ボランティアでは、農家から運営者へ募集依頼が届き、運営者がボランティアへ募集情報を案内する。

参加を希望するボランティアは農家へ連絡し、作業終了後に運営者へ参加内容を報告する。

現行運用には、次の課題がある。

* 参加実績がメールや個別連絡に分散する
* 誰が、いつ、どの農園に参加したか集計しにくい
* ボランティア本人が活動履歴を確認できない
* 運営者による実績集計の負担が大きい
* 継続参加を促す仕組みが少ない
* 地域への貢献が本人に見えにくい

### 2.3 解決方針

農家ごとにQRコードを用意する。

参加したボランティアがLINEでQRコードを読み込み、LIFFアプリ上で参加登録を行う。

登録された参加履歴に応じて、デジタルスタンプを付与する。

専用スマートフォンアプリのインストールを求めず、LINEを利用者向けの入口とする。

---

## 3. 技術構成

MVPでは、次の単純な構成を使用する。

```text
LINE
  ↓
LIFFアプリ
  ↓
Next.js
  ↓
Firebase Firestore
```

使用する技術：

* Webアプリ：Next.js
* 言語：TypeScript
* LINE連携：LIFF SDK
* データベース：Firebase Firestore
* UI：通常のCSSまたはTailwind CSS
* 公開先：Vercel
* LINE側の設定：LINE Developers

Next.jsでは、次の両方を実装する。

* LINE内で表示する画面
* Firestoreへアクセスするサーバー処理

MVPでは、独立したバックエンドサーバーを作成しない。

MVPでは、次のものを使用しない。

* Firebase Authentication
* LINE Messaging API
* Reduxなどの複雑な状態管理
* shadcn/uiなどの追加UIライブラリ
* マイクロサービス
* 有料の外部サービス
* 独立した管理者用システム

必要性が明確になった場合に、後から追加を検討する。

---

## 4. プロジェクトの目的

本システムの目的は次のとおりとする。

1. 援農ボランティアが簡単に参加記録を残せるようにする
2. ボランティア本人が活動履歴を確認できるようにする
3. デジタルスタンプによって継続参加を促す
4. 農家別・参加者別の活動実績を蓄積する
5. 運営者による実績集計の基礎データを作る
6. 将来の募集、マッチング、特典機能の土台を作る

本システムは、次の目的には使用しない。

* 勤怠管理
* 給与計算
* 人事評価
* ボランティア間の優劣判定
* 作業速度の比較

---

## 5. 対象利用者

### 5.1 援農ボランティア

MVPの主な利用者とする。

主な操作：

* LINEからLIFFアプリを開く
* 農園のQRコードを読み込む
* 参加内容を登録する
* 累計参加回数を確認する
* スタンプ数を確認する
* 過去の参加履歴を確認する

### 5.2 農家

MVPでは、農家にシステム操作を求めない。

主な役割：

* 自農園用のQRコードを掲示する
* ボランティアへQRコードの場所を案内する

MVPでは、次を農家へ求めない。

* ログイン
* 参加承認
* 管理画面の操作
* QRコードの毎日更新
* ボランティア評価

### 5.3 運営管理者

想定利用者：

* 市役所職員
* 援農事業の運営担当者
* システム管理者

MVP初期では、専用の管理画面を作らない。

農園情報や参加履歴は、Firebase Consoleなどから確認する。

将来的には管理画面を検討する。

---

## 6. 基本利用フロー

### 6.1 初回利用

1. ボランティアがLINE公式アカウントを友だち追加する
2. リッチメニューまたは案内URLからLIFFアプリを開く
3. LIFFアプリがLINEログイン状態を確認する
4. システムがLINEユーザーを安全に識別する
5. 初回利用の場合はユーザーレコードを作成する
6. ホーム画面を表示する

LINE公式アカウントのリッチメニュー設定は、アプリ本体とは別にLINE公式アカウント側で行う。

### 6.2 参加登録

1. 農家が圃場にQRコードを掲示する
2. ボランティアがLINEでQRコードを読み込む
3. LIFFアプリの参加登録画面を開く
4. システムがQRコード内のトークンを読み取る
5. 対象農園を取得する
6. 農園名を画面に表示する
7. ボランティアが作業内容を選択する
8. ボランティアが作業時間区分を選択する
9. 「参加を記録する」を押す
10. サーバー側で認証、入力値、重複登録を確認する
11. 参加履歴を保存する
12. 累計参加回数を更新する
13. スタンプを1個付与する
14. 登録完了画面を表示する

QRコードを読み込んだだけでは、参加登録を確定しない。

### 6.3 参加履歴確認

1. ボランティアがLIFFアプリを開く
2. ホーム画面を表示する
3. 累計参加回数とスタンプ数を確認する
4. 参加履歴画面を開く
5. 過去の参加履歴を新しい順に確認する

---

## 7. MVPの対象範囲

### 7.1 MVPに含める機能

* LIFFアプリの起動
* LINEログイン状態の確認
* LINEユーザーの識別
* 初回ユーザー登録
* 農園QRコードの読み取り
* QRトークンからの農園取得
* 農園名の表示
* 作業内容の選択
* 作業時間区分の選択
* 参加登録
* 重複登録防止
* 参加履歴の保存
* スタンプ1個の付与
* 累計参加回数の表示
* 累計スタンプ数の表示
* 最近の参加履歴の表示
* 参加履歴一覧の表示
* 無効なQRコードのエラー表示
* 利用停止中農園のエラー表示
* スマートフォン向けUI

### 7.2 MVPに含めない機能

* 募集情報の配信
* ボランティア応募
* 農家とのマッチング
* 農家への直接連絡
* LINEメッセージによる登録完了通知
* LINE Messaging API
* 作業開始・終了の2回打刻
* 分単位の作業時間計測
* 農家による参加承認
* GPS判定
* 日替わりQRコード
* 動的QRコード
* QRコード画像の自動生成
* 商品交換
* 地域通貨
* 決済
* ランキング
* ボランティア評価
* AIによる作業量計測
* 専用管理画面
* CSV出力
* PDF活動証明書

---

## 8. 機能要件

## 8.1 LIFF初期化

### 要件

* LIFF SDKを読み込む
* 環境変数からLIFF IDを取得する
* アプリ起動時に`liff.init()`を実行する
* 初期化中はローディング画面を表示する
* 初期化失敗時は日本語エラーを表示する
* LIFF内ブラウザか外部ブラウザかを考慮する

### エラー表示例

```text
LINEとの接続に失敗しました。
通信環境を確認して、もう一度お試しください。
```

---

## 8.2 LINEユーザー登録

### 要件

* 未ログインの場合はLINE Loginへ誘導する
* LINEユーザーをサーバー側で検証する
* クライアントから送られたLINEユーザーIDをそのまま信用しない
* 初回利用時にユーザーレコードを作成する
* 既存ユーザーの場合は既存レコードを利用する
* LINE表示名とプロフィール画像URLを保存できる
* 最終利用時にプロフィールを更新できる構造とする

### 保存項目

```text
id
lineUserId
displayName
pictureUrl
totalVisits
totalStamps
status
createdAt
updatedAt
```

### 制約

* `lineUserId`は一意とする
* `totalVisits`は0以上とする
* `totalStamps`は0以上とする
* 利用停止ユーザーは参加登録できない

---

## 8.3 農園QRコード

### 要件

* 農園ごとにランダムな`qrToken`を発行する
* 推測可能な農園連番IDを公開URLに使用しない
* QRコードから参加登録画面を開く
* QRコード読取り後に農園名を表示する
* QRコードを読んだだけでは参加を登録しない
* 無効なトークンでは登録できない
* 利用停止中の農園では登録できない

### URL例

```text
https://liff.line.me/{LIFF_ID}/check-in?token={qrToken}
```

実際の形式は、LINEの公式仕様とデプロイ構成に合わせる。

### QRコードに含めない情報

* ボランティアの個人情報
* LINEユーザーID
* LINEアクセストークン
* LINE IDトークン
* チャネルシークレット
* Firebase秘密鍵

---

## 8.4 参加登録画面

### 表示項目

* 農園名
* 作業日
* 作業内容
* 作業時間区分
* 参加登録ボタン

### 農園名

QRトークンから自動取得する。

利用者が農園を自由入力または変更できないようにする。

### 作業日

MVPでは、登録当日を使用する。

利用者による日付変更はできないものとする。

日付の判定はサーバー側でも行う。

### 作業内容

初期候補：

```text
THINNING: 摘果
BAGGING: 袋掛け
HARVESTING: 収穫
PRUNING: 剪定
MOWING: 草刈り
TRAINING: 誘引
FIELD_MAINTENANCE: 圃場整備
OTHER: その他
```

MVPでは、「その他」の詳細入力は任意とする。

### 作業時間区分

```text
MORNING: 午前
AFTERNOON: 午後
FULL_DAY: 終日
```

開始時刻および終了時刻は記録しない。

### ボタン

ボタン名：

```text
参加を記録する
```

ボタン押下後は、処理が完了するまで再押下できないようにする。

---

## 8.5 参加登録処理

### 処理内容

1. LINEユーザーを検証する
2. QRトークンを検証する
3. 農園の利用状態を確認する
4. 作業内容を検証する
5. 作業時間区分を検証する
6. 重複登録を確認する
7. 参加履歴を作成する
8. ユーザーの累計参加回数を1増やす
9. ユーザーの累計スタンプ数を1増やす
10. 登録結果を返す

参加履歴作成と集計値更新は、可能な限りFirestore Transactionで処理する。

### 登録成功時

次を返す。

* 農園名
* 作業日
* 作業内容
* 作業時間区分
* 今回のスタンプ数
* 累計参加回数
* 累計スタンプ数

---

## 8.6 重複登録防止

### 基本ルール

MVPでは、同じユーザーが、同じ農園に、同じ日付で複数回登録できないものとする。

重複判定：

```text
userId + farmId + workDate
```

### 重複時の表示

```text
本日のこの農園への参加は、すでに記録されています。
```

### 実装要件

* サーバー側で判定する
* 同時リクエストでも重複しないようにする
* 重複時は新しい参加履歴を作成しない
* 重複時はスタンプを増加させない
* UI上のボタン無効化だけに依存しない

### 将来拡張

将来的には、午前と午後を別々に登録する可能性がある。

将来の判定例：

```text
userId + farmId + workDate + timeCategory
```

判定ルールを変更しやすい実装にする。

---

## 8.7 スタンプ付与

### 基本ルール

```text
参加登録1件 = スタンプ1個
```

午前、午後、終日による違いは設けない。

### 要件

* 重複登録には付与しない
* 参加履歴とスタンプ更新を一体で処理する
* 登録失敗時にスタンプだけ増えないようにする
* スタンプに金銭的価値を持たせない
* MVPではスタンプ交換機能を作らない

---

## 8.8 ホーム画面

### 表示内容

* LINE表示名
* プロフィール画像
* 累計参加回数
* 累計スタンプ数
* 最近の参加履歴
* 参加履歴画面へのボタン
* 利用案内

### 最近の参加履歴

最大3件を表示する。

表示項目：

* 農園名
* 作業日
* 作業内容
* 作業時間区分

履歴が存在しない場合：

```text
まだ参加記録はありません。
農園のQRコードから参加を記録できます。
```

---

## 8.9 参加履歴画面

### 要件

* ログイン中の本人の履歴だけを表示する
* 新しい履歴から順に表示する
* 他人の履歴を表示しない
* 初期段階では最新50件程度までの表示でもよい
* データ増加時にページネーションを追加できる構造とする

### 表示項目

* 農園名
* 作業日
* 作業内容
* 作業時間区分
* 付与スタンプ数

---

## 8.10 登録完了画面

### 表示内容

```text
参加を記録しました
```

あわせて次を表示する。

* 農園名
* 作業日
* 作業内容
* 作業時間区分
* 今回付与されたスタンプ数
* 累計参加回数
* 累計スタンプ数

ボタン：

* ホームへ戻る
* 参加履歴を見る

---

## 8.11 エラー処理

次の場合に日本語エラーを表示する。

* LIFF初期化失敗
* LINEログイン失敗
* LINEユーザー検証失敗
* QRトークン未指定
* QRトークン無効
* 農園が存在しない
* 農園が利用停止中
* 必須項目未入力
* 不正な作業内容
* 不正な作業時間区分
* 重複登録
* Firestore保存失敗
* 通信エラー
* システムエラー

内部エラーの詳細、スタックトレース、秘密情報は画面に表示しない。

---

## 9. 画面要件

## 9.1 画面一覧

MVPでは次の画面を作成する。

1. 初期化中画面
2. ホーム画面
3. 参加登録画面
4. 登録完了画面
5. 参加履歴画面
6. エラー画面
7. 利用案内画面

ログイン専用画面は必要に応じて追加する。

---

## 9.2 UI方針

* スマートフォン表示を優先する
* LINE内ブラウザで利用しやすくする
* 320px程度の画面幅でも利用できるようにする
* 390px前後を標準確認サイズとする
* 文字を小さくしすぎない
* ボタンを押しやすい大きさにする
* 画面内の選択肢を増やしすぎない
* 重要な操作を一画面にまとめる
* 処理中はローディング表示を出す
* 登録ボタンの二重押下を防止する

### デザイン方針

* 緑色を基本色にする
* 白い背景を基本とする
* 明るく親しみやすくする
* 果物や葉のモチーフは補助的に使用する
* 過度にゲーム的な表現を避ける
* 高齢者にも分かりやすい日本語を使用する
* ランキングや競争を表示しない

MVPでは、追加のUIコンポーネントライブラリを必須としない。

通常のCSSまたはTailwind CSSで実装する。

---

## 10. データ要件

## 10.1 usersコレクション

想定項目：

```text
id
lineUserId
displayName
pictureUrl
totalVisits
totalStamps
status
createdAt
updatedAt
```

### status候補

```text
ACTIVE
SUSPENDED
```

### 制約

* `lineUserId`は一意
* `totalVisits`は0以上
* `totalStamps`は0以上
* `SUSPENDED`のユーザーは参加登録不可

---

## 10.2 farmsコレクション

想定項目：

```text
id
name
ownerName
qrToken
isActive
createdAt
updatedAt
```

任意項目：

```text
fruitTypes
description
```

### 制約

* `qrToken`は一意
* `qrToken`は推測困難なランダム値
* `isActive`がfalseの場合は参加登録不可
* 住所はMVPでは必須としない
* 農家の電話番号などは保存しない

---

## 10.3 participationsコレクション

想定項目：

```text
id
userId
farmId
workDate
workType
timeCategory
stampsGranted
createdAt
```

任意項目：

```text
otherWorkDescription
status
```

### 制約

* `userId`は存在するユーザーを参照する
* `farmId`は存在する農園を参照する
* `stampsGranted`はMVPでは1
* 同一ユーザー、同一農園、同一日付の重複を防止する
* 作業日はサーバー側で確定する

### 農園名の保持

農園名を`participations`にも保存するかは、実装時に判断する。

履歴表示時の性能と、農園名変更後の履歴表示を考慮する。

---

## 11. セキュリティ要件

* クライアントから送信されたLINEユーザーIDを信用しない
* LINE IDトークンまたはアクセストークンをサーバー側で検証する
* チャネルシークレットをブラウザへ渡さない
* Firebase秘密鍵をブラウザへ渡さない
* 秘密情報をGitHubへコミットしない
* 秘密情報は環境変数で管理する
* `.env.local`をGit管理しない
* QRコードに個人情報を含めない
* QRコードをユーザー認証として扱わない
* 参加登録APIで入力値を検証する
* 参加登録APIで重複を確認する
* 参加履歴は本人だけが取得できるようにする
* 内部エラーを利用者へそのまま表示しない
* トークン全文をログへ出力しない
* 個人情報の保存を必要最小限にする

---

## 12. 不正利用対策

MVPで実施する。

* 農園ごとのランダムQRトークン
* 同一農園への1日1回制限
* LINEユーザーのサーバー側検証
* 無効農園の拒否
* 利用停止農園の拒否
* サーバー側の入力検証
* 登録ボタンの二重押下防止
* Firestore Transaction
* 重複登録のログ記録

MVPでは実施しない。

* GPS
* 農家の承認
* 日替わりQRコード
* ワンタイムQRコード
* 募集申込者との照合
* 指定時刻内のみ登録
* 顔認証
* NFC

スタンプに金銭的価値を持たせる場合は、追加の不正対策を検討する。

---

## 13. 非機能要件

### 13.1 性能

* 一般的なスマートフォン回線で利用できる
* 不要に大きな画像を使用しない
* 参加登録は通常数秒以内に完了する
* 初期表示に不要なデータを大量取得しない

### 13.2 保守性

* TypeScriptを使用する
* LINE処理とFirestore処理を分離する
* UIと業務ロジックを可能な範囲で分離する
* 作業内容と時間区分を一か所で管理する
* 環境変数を`.env.example`に記載する
* 主要な処理に適切なエラー処理を行う
* 過剰なライブラリを追加しない

### 13.3 可用性

* 通信失敗時に再操作できる
* 処理結果が不明な場合でも二重登録を防止する
* Firestoreエラー時にスタンプだけ増加しない
* エラー時に分かりやすい案内を表示する

### 13.4 ログ

記録する。

* 参加登録成功
* 参加登録失敗
* 重複登録
* 無効なQRトークン
* LINE認証失敗
* Firestoreエラー

記録しない。

* LINE IDトークン全文
* LINEアクセストークン全文
* チャネルシークレット
* Firebase秘密鍵
* 不要な個人情報

---

## 14. テスト要件

最低限、次をテストする。

* 有効なQRトークンから農園を取得できる
* 無効なQRトークンを拒否できる
* 利用停止中の農園を拒否できる
* 新規ユーザーを登録できる
* 既存ユーザーを再利用できる
* 参加履歴を保存できる
* スタンプを1個加算できる
* 重複登録を防止できる
* 重複時にスタンプが増加しない
* 不正な作業内容を拒否できる
* 不正な時間区分を拒否できる
* 登録失敗時に集計値だけ更新されない
* 本人以外の参加履歴を取得できない

可能であれば、次も確認する。

* Next.jsのビルド
* TypeScriptの型チェック
* lint
* スマートフォン表示
* LINE内ブラウザでの実機動作

---

## 15. 初期データ

開発環境では、次のサンプル農園を用意する。

```text
川崎果樹園
稲城梨園
多摩桃園
```

これらは開発用の仮データとし、実在農園の正式情報として扱わない。

各農園にランダムな`qrToken`を設定する。

---

## 16. 環境変数

想定する環境変数：

```text
NEXT_PUBLIC_LIFF_ID
LINE_CHANNEL_ID
LINE_CHANNEL_SECRET
FIREBASE_PROJECT_ID
FIREBASE_CLIENT_EMAIL
FIREBASE_PRIVATE_KEY
```

必要に応じて追加してよいが、追加理由をREADMEに記載する。

`.env.example`には次の形式で記載する。

```text
NEXT_PUBLIC_LIFF_ID=
LINE_CHANNEL_ID=
LINE_CHANNEL_SECRET=
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
```

実際の値は記載しない。

---

## 17. 将来機能

### 17.1 募集・マッチング

* 農家による募集登録
* LINEへの募集通知
* ボランティアの参加申込
* 定員管理
* キャンセル管理
* 農家との連絡

### 17.2 農家向け機能

* 参加者一覧
* 参加承認
* 「ありがとう」の送信
* QRコードの再発行
* 年間受入実績

### 17.3 運営者向け機能

* 管理ダッシュボード
* 農園別集計
* 月別集計
* 年度別集計
* CSV出力
* 活動証明書
* 誤登録の取消

### 17.4 ボランティア向け機能

* バッジ
* 品目別スタンプ
* イベント案内
* 特典
* 活動証明
* 次回募集表示

### 17.5 LINE Messaging API

* 登録完了通知
* 募集通知
* リマインド
* スタンプ獲得通知
* 農家からの感謝メッセージ

---

## 18. 未確定事項

次は、実証実験前または実装中に決定する。

* アプリの正式名称
* 運営主体
* LINE公式アカウントの管理者
* 利用規約
* プライバシーポリシー
* データ保存期間
* 退会時の削除方法
* スタンプの有効期限
* 「その他」の詳細入力
* 参加記録の取消方法
* 誤登録の修正方法
* 農園名を参加履歴に複製保存するか
* CSSかTailwind CSSか
* Firebase Emulatorを使用するか
* 管理画面を将来AppSheetで作るか
* 既存AppSheetとのデータ連携方法
* LINE公式アカウントのリッチメニュー構成

未確定事項があっても開発を停止せず、合理的な仮定を明示してMVPを進める。

ただし、セキュリティまたはデータ消失に関係する事項は、推測のみで本番実装しない。

---

## 19. 受入条件

MVPは、次の条件を満たした場合に完了とする。

1. LINEからLIFFアプリを起動できる
2. LINEユーザーをサーバー側で安全に識別できる
3. 有効な農園QRコードから参加登録画面を開ける
4. 農園名を正しく表示できる
5. 作業内容を選択できる
6. 作業時間区分を選択できる
7. 参加履歴をFirestoreへ保存できる
8. 同一日、同一農園への重複登録を防止できる
9. 参加時にスタンプを1個付与できる
10. 累計参加回数を表示できる
11. 累計スタンプ数を表示できる
12. 本人の参加履歴を表示できる
13. 無効なQRコードを拒否できる
14. 利用停止中の農園を拒否できる
15. 秘密情報がGitHubへコミットされていない
16. TypeScriptの型チェックが成功する
17. lintが成功する
18. 主要な自動テストが成功する
19. Next.jsのビルドが成功する
20. READMEに起動・設定・デプロイ手順が記載されている

---

## 20. 要件の優先順位

### Must

* LIFF起動
* LINEユーザー識別
* QRトークンによる農園取得
* 参加登録
* 重複登録防止
* スタンプ付与
* 参加履歴表示
* セキュリティ対策

### Should

* ホーム画面
* プロフィール表示
* 最近の参加履歴
* 分かりやすいエラー表示
* 自動テスト
* 開発用初期データ

### Could

* 利用案内画面
* その他作業の詳細入力
* 簡易的なQRコード生成
* Firebase Emulator
* 簡易管理機能

### Won't in MVP

* 募集・応募
* マッチング
* Messaging API
* 管理ダッシュボード
* GPS
* 動的QRコード
* 特典交換
* 決済
* ランキング
* 作業量のAI計測

---

## 21. 更新履歴

| 日付         | バージョン | 変更内容                              | 更新者 |
| ---------- | ----: | --------------------------------- | --- |
| 2026-08-01 |   0.2 | 技術構成をNext.js、LIFF、Firestore中心に簡略化 | 未設定 |
